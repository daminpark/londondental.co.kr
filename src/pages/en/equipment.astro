---
import { getEntry, render } from 'astro:content';
import MarkdownLayout from '../../layouts/MarkdownLayout.astro';

const entry = await getEntry('pages', 'en/equipment');
const { Content } = await render(entry!);
---

<MarkdownLayout title={entry!.data.title} description={entry!.data.description} lang="en" canonicalPath="/en/equipment">
  <Content />
</MarkdownLayout>
