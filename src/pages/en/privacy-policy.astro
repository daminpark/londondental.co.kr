---
import { getEntry, render } from 'astro:content';
import MarkdownLayout from '../../layouts/MarkdownLayout.astro';

const entry = await getEntry('pages', 'en/privacy-policy');
const { Content } = await render(entry!);
---

<MarkdownLayout title={entry!.data.title} description={entry!.data.description} lang="en" canonicalPath="/en/privacy-policy">
  <Content />
</MarkdownLayout>
